<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Update Student</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <nav>
        <a href="/">Home</a> |
        <a href="/students">View Students</a> |
        <a href="/students/add">Add Student</a> |
        
        <% if (loggedIn) { %> <!-- Only show logout if logged in -->
            <a href="/logout">Logout</a> |
        <% } %>
    </nav>

    <h1>Update Student</h1>

    <form action="/students/update/<%= student._id %>" method="POST">
        <input type="text" name="name" value="<%= student.name %>" required><br><br>
        <input type="number" name="rollNumber" value="<%= student.rollNumber %>" required><br><br>
        <input type="text" name="class" value="<%= student.class %>" required><br><br>
        <input type="text" name="section" value="<%= student.section %>" required><br><br>
        <input type="date" name="dateOfBirth" value="<%= student.dateOfBirth.toISOString().split('T')[0] %>" required><br><br>
        <button type="submit">Update Student</button>
    </form>

</body>
</html>
